# AUDIT.md — QuantGuard 비판적 재감사 런북

> **언제 이 문서를 쓰나:** 사용자가 "비판적으로 봐줘 / 문제점 찾아줘 / 분석 평가 / 안정성 점검"
> 등을 요청할 때. **매번 처음 보는 사람처럼, 문제를 찾는 것 위주로** 감사한다.
> 목표는 새 기능이 아니라 **실돈을 지키는 안정성 완성**이다.
>
> 이 문서는 세 부분이다 — ① **감사 방법(고정 절차)**, ② **발견 이력 원장(매번 갱신)**,
> ③ **주간 신호 검증 루프**(순위 예측력 — 매주 기록, 누적으로만 판정).
> 원장이 있어서 같은 곳을 두 번 훑지 않고, 아직 안 본 곳·미해결 항목으로 바로 간다.
>
> **감사(①②)는 *코드 결함*을, 루프(③)는 *신호 품질*을 본다.** 둘을 섞지 말 것 —
> 순위가 안 맞은 것은 대개 버그가 아니라 국면이다.

---

## ① 감사 방법 (매번 이 순서)

### 원칙 (마인드셋)
1. **처음 보는 눈.** 기존 주석의 "이건 fix13에서 고쳤음"을 믿지 말고 코드가 실제로 그렇게
   동작하는지 실행경로를 직접 읽어 확인한다.
2. **돈-리스크 우선.** 신호 정확도보다 **실행부 버그가 실돈을 더 크게 날린다.** 감사 순서는
   아래 컴포넌트 리스크 맵의 💰 높은 순.
3. **fail-safe 검증 필수.** 결함을 고치기 전에 "이 경로가 실패하면 시스템이 안전한 쪽으로
   끝나는가"를 먼저 확인한다. (예: A가 중단→시그널 미갱신→B가 `STALE_SIGNAL_ABORT`로
   포지션 유지 = 안전). 고치는 방향도 **fail-safe**(의심스러우면 매매 스킵)로 잡는다.
4. **신호/전략 파라미터는 동결.** 모멘텀 기간·임계값·재진입·콤보는 백테스트 3연속 검증
   탈락 이력이 있어 **손대지 않는다.** 감사에서 고치는 건 *신뢰성*(가드·검증·중복제거)만.
   ([[signal-tuning-freeze]] 참조)
5. **한 증상만 고치고 끝내지 않는다.** 오매칭 고치면 미매칭 회귀 확인, 그 반대도.
   실제 운영 데이터로 수정 전/후 비교 전엔 완료 처리 금지. (CLAUDE.md "수정 완료 판정 기준")
6. **감사는 1회로 끝나지 않는다 — 이미 본 곳을 다시 파는 게 더 깊다.** 첫 감사는 구조적·
   명백한 결함을 걷어내고, 재감사는 그때 놓친 미묘한 걸 잡는다. **증거:** fix23·fix24는 둘 다
   "이미 감사한" rambdaB·signal_generator를 **다시 판** 데서 나왔다. 또 **내 수정 자체가 새
   코드 표면을 만들므로**(가드·재시도·통합 코드) 고친 컴포넌트는 반드시 다음에 재감사한다.
   재감사는 **같은 렌즈 반복이 아니라 다른 렌즈로 심화**한다(아래 "감사 렌즈").

### 감사 렌즈 (패스마다 다른 관점 — 재감사는 아직 안 쓴 렌즈로)
- **L1 구조·제어흐름**: 함수 분기, `except: continue`, 조용한 실패, 중복.  ← 대개 1차
- **L2 수치·경계**: 0 나눗셈, 빈 시리즈, off-by-one, 임계 등호(`<=` vs `<`), 반올림.
- **L3 데이터 신선도·정합성**: stale(있지만 낡음), 미래 날짜, 음수, 단위 혼동, 타임존.
- **L4 상태·순서·동시성**: 재실행/중복, 부분 실패 후 상태, 읽기-쓰기 순서, S3 경합.
- **L5 계약·스키마**: KIS/야후 실제 필드명·응답코드가 코드 가정과 일치하나(실캡처 대조).
- **L6 시스템 통합·전체 흐름**(컴포넌트 경계를 가로지름 — 작은 부분이 각각 옳아도 크게 보면
  깨지는 것): ⓐ **필드 계약** A가 S3에 쓰는 필드 ↔ B가 읽는 필드(이름·타입·유무) 대조,
  ⓑ **타이밍/순서** A→B 실행 간격 vs A 소요시간, 신선도 임계, 쓰기-읽기 경합, ⓒ **상태 전이**
  BEAR↔BULL 재진입·carry-over가 두 Lambda·주(週) 사이로 올바로 흐르나, ⓓ **관측성** 조용한
  스킵/실패로 "매매가 안 된 사실"을 사용자가 모르는 공백은 없나(경고 일관성), ⓔ **소비자 계약**
  B가 쓰는 S3(latest_signal 등) ↔ analyze_returns/dashboard가 읽는 필드 정합.
> **L1~L5는 컴포넌트별 현미경, L6는 전체 흐름 망원경.** 컴포넌트를 "감사 완료"로 보려면
> L1~L5를 각각 통과해야 하고, **시스템 전체는 별도로 L6를 주기적으로** 돈다. 원장 "렌즈" 칸에
> 적용 렌즈를 기록하고 빠진 걸 고른다.
> **증거 — fix30·fix31 둘 다 L6에서 나왔다.** 개별 return·계산은 각각 옳았지만 큰 그림에선
> '매매 미실행 침묵'(ⓓ)·'관측 지표가 조용히 거짓'(ⓔ)이 결함이었다. 컴포넌트 감사만
> 반복하면 이 클래스는 영영 안 잡힌다.

### 절차 (STEP)
- **STEP 0. 이번 패스 대상 1개 선정** — 아래 우선순위로 고른다(breadth·depth 균형):
  1. **미해결/보류 항목**(#OPEN-*)이 있으면 우선.
  2. **직전에 내가 수정한 컴포넌트**(fix가 새 표면을 만듦) 중 재감사 안 한 것.
  3. 원장에서 **L1~L5 렌즈가 아직 다 안 채워진 💰 컴포넌트** → 빠진 렌즈로 심화 재감사.
  4. 위가 없으면 **가장 오래 안 본 💰 컴포넌트**(미감사 우선).
  > 매번 새 곳만 가지 말 것. **높은 돈-리스크 컴포넌트는 렌즈를 바꿔가며 반복** 감사하고,
  > **3~4개 훑을 때마다 한 번은 대상을 "시스템 전체"로 잡아 L6를 돈다.**
- **STEP 1.** 그 컴포넌트의 **실행경로를 실제로 정독**한다(grep로 함수 위치 → Read).
  아래 "버그 클래스 체크리스트"를 대조하며 읽는다.
- **STEP 2.** 의심 결함마다 **구체적 실패 시나리오**(입력→잘못된 출력)를 적는다. 시나리오를
  못 만들면 결함이 아니다(추측 금지).
- **STEP 3.** 결함의 **fail-safe 여부**를 확인한다. 이미 안전하게 끝나면 우선순위를 낮춘다.
- **STEP 4.** 고칠 것을 **하나** 골라(가장 큰 돈-리스크 × 낮은 변경위험) 수정 + **회귀 테스트**
  작성. 테스트는 **실제 사고 시나리오를 재현**해야 한다(통과만 하는 테스트 금지).
  외부 API mock 필드는 코드에서 베끼지 말고 독립 근거로(CLAUDE.md "외부 API 목킹 원칙").
- **STEP 5.** 전체 테스트 통과 + `py_compile` → `/log/` JSON → 커밋/푸시(CI 게이트) →
  Notion HISTORY 새 항목 → (안전장치/로직 변경 시) SPECIFICATION 갱신.
- **STEP 6.** **이 문서 ②원장을 갱신** — 발견 항목 추가, 감사 커버리지 표의 **적용 렌즈·
  날짜** 갱신, 이번에 수정한 컴포넌트는 **"재감사 필요" 표시**(내 fix가 새 표면을 만듦),
  보류 항목 정리, **이번 수정이 도달한 검증 강도(V?) 기록**. (빼먹으면 다음 감사가 무의미하다.)

### 검증 강도 사다리 (V1~V6) — 매 패스 더 강한 방법을 제안·추가
> STEP 4에서 테스트를 짤 때, **지금 도달한 강도보다 한 칸 위가 싸게 가능하면 제안하고 추가**한다.
> 검증 방법 자체를 진화시킨다(렌즈가 "어디를 볼까"라면, 이건 "얼마나 강하게 증명할까").
> 매 fix는 최소 V1을 넘고, **돈-리스크 💰💰💰는 V2 이상**을 목표로 한다.
- **V1 로직 단위테스트(mock)** — 분기·계산 검증. *한계: 스키마 미검증(mock이 코드와 순환일 수 있음).*
- **V2 변이-구별 테스트** — 구버전(버그 버전)이면 실패하는 케이스를 명시(예: rt_cd 오류인데 값 있는 응답
  거부). "통과만 하는 테스트" 방지. (fix23 적용)
- **V3 구조·불변식(identity) 테스트** — 단일소스·보존식 보장(예: `korea.execute_order is
  kis_common.execute_order`). 리팩토링 회귀를 구조로 못박음. (fix23 적용)
- **V4 경계 스윕 / property 테스트** — 한 입력이 아니라 다수 입력에 대해 불변식 검증
  (예: 여러 가격에 대해 `calc_limit_price`가 항상 5원 배수·매수는 현재가 이상).
  *도입 완료 — fix26(경계 스윕)·fix27(속성)·fix28~30(e2e 불변식).*
- **V5 실캡처 골든 응답 대조** — `docs/`의 **실제 KIS/야후 응답 JSON**을 mock 근거·골든으로 써서
  스키마까지 검증(필드명 오독=fix17/18류 재발 방지). CLAUDE.md 목킹 원칙 2번과 직결.
  ***미도입 → 다음 목표.*** (#OPEN-V)
- **V6 실환경 콘솔 검증** — `FORCE_TEST_MODE=True` + `force_run`으로 실제 KIS/S3 경로 왕복.
  스키마·권한·경합까지 실검증. 사람 개입 필요 → 스키마 신뢰가 중요한 신규 API에서만.

### 버그 클래스 체크리스트 (읽으며 대조)
- [ ] **조용한 부분 실패** — 루프에서 `except: continue` 후 결과 개수/커버리지 검증 없이 진행?
      (fix24 사고: 배치 절반 실패해도 반쪽으로 매수)
- [ ] **신선도(stale) 미검증** — 데이터가 *있지만 낡은* 경우를 그냥 쓰나? (empty/예외만 잡고
      오래된 마지막 값 사용) → 잘못된 BULL/BEAR·모멘텀.
      (DD가드 fix22·VIX fix25·모멘텀 fix26으로 3경로 커버됨 — **신규 데이터 경로에만 적용**)
- [ ] **응답코드 미검증** — KIS `rt_cd`를 확인 안 하고 `output`만 읽나? 유량초과 응답을
      정상처럼 반환? (fix14/23 하드닝 대상)
- [ ] **체결 가정** — 주문 접수(rt_cd=0)를 체결로 간주하나? 미체결 시 후속 계산이 틀어지나?
      (매도는 체결폴링 있음, **매수는 접수=완료로 간주 — 미해결 #OPEN-1**)
- [ ] **fail-open** — 데이터/조회 실패 시 위험한 쪽(BULL 개방·전량 매수)으로 기본값? (fix15 VIX)
- [ ] **나눗셈/인덱스 edge** — 0 나눗셈, 빈 시리즈 `.iloc[-1]`, base==0, 데이터 1행.
- [ ] **타임존/기준일** — `datetime.today()`(UTC Lambda) vs KST, `last_friday` 요일 계산,
      `<= last_friday` 필터 일관성.
- [ ] **중복/동기화** — 같은 로직이 두 곳에 복붙돼 수동 동기화 중? (fix23 단일소스화)
- [ ] **테스트 격리** — 테스트 실행이 실전 아카이브/시그널을 덮나? (fix22 s3_keys)
- [ ] **회귀 방향** — 정밀도 고치며 재현율 깨지 않았나(그 반대도)? 실운영 데이터로 비교했나?
- [ ] **[L6] 필드 계약** — A가 S3에 쓰는 필드명/타입 ↔ B(및 analyze_returns/dashboard)가 읽는
      필드가 정확히 일치하나? 한쪽만 바꾸면 조용히 깨진다(기본값 폴백에 묻힘).
- [ ] **[L6] 조용한 미실행 관측성** — 매매를 스킵/중단하는 return 경로마다 사용자가 그 사실을
      **알 수 있나**(텔레그램 등)? 경고가 result별로 일관적인가? (fix30: STALE/S3_ERROR/NO_TARGETS가
      침묵이었음. 각 return은 옳아도 큰 그림에선 '매매 안 된 걸 모름'이 결함) — 스킵인데 조용하면 의심.
- [ ] **[L6] 타이밍/순서** — A→B 실행 간격이 A 소요시간보다 빠듯한가? 신선도 임계가 그 간격과
      정합한가? 쓰기 전에 읽는(경합) 순서는 없나?
- [ ] **[L6] 상태 전이** — BEAR↔BULL 재진입·VIX carry-over가 두 Lambda·주(週) 경계로 올바로
      흐르나? 한 주 스킵되면 다음 주 기준(prev_equity 등)이 어긋나지 않나?

---

## ② 발견 이력 원장 (매번 갱신) — 최종 갱신 2026-08-23 (fix33 관측 2종: 체결감사·가드후보 2개)

### 컴포넌트 리스크 맵 & 감사 커버리지
> "적용 렌즈"에 L1~L5 중 이미 통과한 것을 적는다. 다섯 개가 다 차야 "감사 완료".
> `↻` = 내 fix가 새 표면을 만들어 **재감사 필요**.
>
> | 컴포넌트 | 돈-리스크 | 최근 | 적용 렌즈 | 남은/상태 |
> |---|---|---|---|---|
> | `rambdaB/korea.py` (집행) | 💰💰💰 | 08-23 | L1,L2✓(재투입+메인 사이징),L4(매도분기),L5,L6ⓓ✓(체결 관측) | L3 남음. ↻(fix33 후). #OPEN-1 관측만 해결 |
> | `rambdaB/lambda_function.py` | 💰💰💰 | 08-04 | L1,L2✓,L4✓,L5,L6ⓓ✓(fix30) | L3 남음. ↻(fix30 후). korea_time KST 확인 |
> | **시스템 전체 (A↔B↔S3↔분석)** | 💰💰💰 | 08-04 | L6ⓐ✓(필드계약)ⓓ✓(관측성) | L6ⓑ타이밍·ⓒ상태전이·ⓔ소비자계약 남음 |
> | `rambdaB/kis_common.py` | 💰💰💰 | 08-03 | L1,L5 | L2·L4 남음. ↻(신규 통합코드) |
> | `rambdaA/signal_generator.py` | 💰💰 | 08-22 | L1,L3✓(DD·VIX·모멘텀),L6ⓒ✓(BEAR→BULL 실전이 실측) | L2·L4·L5 남음. **#OPEN-B 우선순위 상향(상)**·#OPEN-R 표본1 |
> | `rambdaA/yf.py` (야후 수집) | 💰💰 | 08-03 | L3,L5 | L2(길이불일치)·L4 남음. start/end 무시·range 하드코딩 확인 |
> | `rambdaA/fdr.py` (ETF 목록) | 💰💰 | 부분 | L1(부분) | L2~L5 남음(fix20 정렬만) |
> | `rambdaA/data_guard.py`/`s3_keys.py` | 💰 | 08-03 | L1,L2 | L3 남음(신규 fix22) |
> | `backtest/shadow_forward.py`+`guards.py`+`scripts/notify_telegram_shadow.py` | 🚫 종이(관측) | 08-23 | L6ⓔ✓(소비자계약·결측표기, fix31), L2✓(가드 등가성 스윕, fix33) | 자본 0이나 **승격 판단 근거**라 거짓 지표는 위험 |
> | `dashboard/`·`paper_trader/`·나머지 `backtest/`·`scripts/` | 🚫 종이 | — | — | 실돈 아님 — 후순위 |

> **다음 감사 후보(우선순):**
> ~~① 관측 2종(#OPEN-S·#OPEN-B)~~ → **fix32 완료.** 이제 표본이 쌓이길 기다릴 뿐 조치 없음.
> ① **#OPEN-B 경보 설계 보강**(2026-08-22 신규) — 상관 단독 임계로는 이번 괴리를 못 잡았다.
>    베타 하한·주간 레벨 괴리 추가. 관측 전용, 매매 로직 무변경 →
> ② **L6 잔여** — ⓑ타이밍은 2026-08-14 실측 종결(A→B 20분 vs 임계 3600초), ⓔ소비자 계약
>    (latest_signal ↔ analyze_returns) 남음 →
> ③ `rambdaA/fdr.py`(ETF 목록) **L2~L5**(fix20 정렬만 봄) →
> ④ `yf.py` **L2**(길이 불일치 — 2026-08-22 실측: 티커 1개 드롭 시 `res_df.columns = tickers`가
>    ValueError로 **터진다**. 조용한 오라벨은 아니나 호출부가 삼키면 조용한 결측이 됨)·L4 →
> ⑤ **#OPEN-V**(실캡처 골든 → KIS 스키마 V5).
>
> ⚠️ 실행부 3대 모듈 검증 격차 대부분 종결 — 이후는 **L6 통합·데이터 입력단(fdr/yf)·스키마(V5)**에 집중.
> ⚠️ **신호·가드에서 지금 손댈 수 있는 것은 "관측"뿐이다.** #OPEN-R(재진입)·#OPEN-S(모멘텀
> 파라미터)는 [[signal-tuning-freeze]] 동결 대상 — 섀도우 OOS 판정 대기이며 백테스트 재튜닝 금지.

### 미해결 / 보류 항목 (다음 감사에서 우선 검토)
> 종결분은 여기서 지우고 "처리 완료 이력" 표에만 남긴다(중복 방지).
> **최근 종결:** #OPEN-2/2b(stale) → fix25·26, #OPEN-E2E(사이징) → fix28.

**[관측 구현됨 — 표본 누적 대기 중(fix32). 판정 전까지 조치 금지]**
- **#OPEN-S 순위(모멘텀) 신호 품질** — 순위 기준은 **126일 가격모멘텀 단일 팩터** 내림차순.
  **fix32로 주간 자동 관측 시작**(IC·분위·스프레드 → `signal_quality_ledger.json`).
  현재 **1/26주 누적**, 판정 보류. → ③ 루프 참조.
  *2026-08-10 기준선(다음 측정 시 비교용):* 74주 표본(현 유니버스 고정, **생존편향 있음**)에서
  IC 평균 **+0.064** / 중앙값 +0.151 / 표준편차 0.398, **IC>0 비율 62.2%**, IR +0.160,
  top10−유니버스 주간 스프레드 평균 **+0.615%p**(승률 55.4%). → **약하지만 유효**.
  *국면 의존이 큼:* 2026Q2 IC +0.273 / **2026Q3 IC −0.415**(폭락→반등). 실측 OOS 1구간
  (08-03→08-10)은 **IC −0.759**, top10 +0.86% vs 유니버스 +6.34%(스프레드 −5.49%p) —
  단 이 주는 BEAR 현금이라 **실제로 맞지 않았다**(가드가 모멘텀 최악 국면을 차단).
  *판단:* 모멘텀 기간·팩터 구성 변경은 [[signal-tuning-freeze]] **동결 대상**. 착수 가능한 것은
  **관측(IC 기록·경보)뿐**이며, 수정은 ③의 발동 기준을 넘고 섀도우 검증을 통과할 때만.
- **#OPEN-B 가드 대리지표(KODEX200) 가정이 미검증·미관측** (signal_generator DD가드) —
  DD가드는 KODEX200 낙폭으로 **포트폴리오 위험을 대리**하는데, 실매매 대상은 코스피200이
  아니라 섹터/테마 ETF(반도체·2차전지·화장품·자동차 등) top10이다. 둘의 연동은 설계된
  성질이 아니라 "거래대금 상위 100 + 레버리지/채권 제외" 규칙의 **부수 효과**다.
  *2026-08-10 실측(현재는 타당):* top10 동일가중 vs KODEX200 **상관 0.97 / 베타 0.84**,
  일변동성은 포트가 오히려 낮음(4.18% vs 4.79%), 폭락구간(6/26→7/20) 낙폭 -23.50% vs
  -24.23%(차이 0.7%p). → 대리지표로 충분.
  *위험:* 시장 주도주가 코스닥 테마·해외노출 ETF로 이동해 상관이 무너지면 KODEX200 -8%는
  **포트가 훨씬 더 빠진 뒤에야** 켜진다. 그런데 이를 감시하는 장치가 전혀 없다(조용한 열화).
  *조치(fix32 완료):* 주간 top10↔KODEX200 60일 상관·베타를 산출해 원장에 기록하고,
  **상관 < 0.80이면 텔레그램 경고**. 정상이면 침묵(경보 피로 방지). 매매 로직 무변경.
  *판단:* [[signal-tuning-freeze]] **동결 대상 아님** — 파라미터 튜닝이 아니라 가정 검증
  (동결 메모가 권한 "신뢰성" 방향). 다만 현재 전제가 깨지지 않았으므로 우선순위 중.
  재현: `백테스트 없이` quant_signals top10 + yf 종가로 상관/베타 산출(2026-08-10 세션 방식).
  **▶ 2026-08-22 재측정 — 우선순위 상향(중→상). 경보 설계 결함 1건 발견.**
  8/18 재진입 직후 3거래일(8/19~8/21) 실측: **KODEX200 +1.32% vs 유니버스(89종 동일가중)
  −2.00%**, 실보유 top10 −1.67%. 지수는 올랐는데 **매매 가능한 시장 전체가 내렸다**.
  일별 괴리는 8/20 −3.20%p, 8/21 −3.33%p. 60일 상관은 0.97→**0.93**(경보선 0.80 위, 침묵),
  60일 **베타는 0.84→0.66**으로 급락(유니버스 기준 0.66).
  *즉 이번 주 실손실의 주범은 신호도 집행도 아니라 이 대리지표 괴리다.* DD가드는
  "KODEX200이 20일 고점 −8% 위로 회복" 을 보고 재진입했는데, 정작 사는 대상(섹터·테마 ETF)은
  회복하지 않은 상태였다.
  *경보 설계 결함:* fix32 관측은 **상관만** 임계(<0.80)로 감시한다. 상관이 높아도 **베타가
  0.66이면 대리지표는 위험 크기를 40% 과소평가**하고, 이번처럼 **레벨(누적 수익률)이
  3%p 갈라져도** 상관은 0.93으로 조용하다. → 관측 항목에 **베타 하한**과 **주간 레벨 괴리
  (|top10 − KODEX200|)** 추가 검토 필요. 매매 로직은 여전히 무변경(관측만).
**[동결 대기 — 섀도우 OOS 판정 전까지 손대지 않음]**
- **#OPEN-R 재진입이 '회복'이 아니라 '고점 망각'으로 발동** (signal_generator DD가드) —
  재진입 조건이 롤링 20일 고점 대비 -8% 회복인데, 창이 굴러가며 고점이 밀려나면 **지수가
  1원도 오르지 않아도** dd가 임계 위로 올라온다. 2026-08-10 실측: 8/10 수준 완전 횡보 시
  **14거래일(약 2주) 뒤 자동 BULL 전환**, 그때 절대고점 대비 낙폭은 **-33.6%**.
  *판단:* **고치지 않는다(의도적 보류).** 해법 후보인 SMA120 콤보는 이미 검증 탈락
  (지수 개선/실포트 중립)이고, [[signal-tuning-freeze]] 동결 정책 대상. 바닥 재진입은
  whipsaw일 수도 반등 포착일 수도 있어 부호가 정해지지 않았다. → **섀도우 OOS로 판정**:
  `baseline(DD가드)` vs `baseline+콤보가드`가 재진입 국면에서 처음 갈리므로, 2026-08월 하순
  성적표를 이 관점으로 볼 것. 백테스트로 재튜닝 금지.
  **▶ 2026-08-23 fix33 — 판정용 후보 2종 투입(프로덕션 무반영).**
  `backtest/guards.py` 신설, 섀도우 가드를 `use_sma: bool` → 가드 객체로 전환.
  - `baseline+σ임계가드` : 임계 = −K·σ₂₀·√20. **K는 손으로 고르지 않았다** —
    폭락 이전 평상시 σ(2.82%)에서 현행 −8%를 재현하는 값으로 역산해 K=0.6343.
    평상시엔 현행과 동일, 고변동 국면에서만 자동으로 넓어진다.
  - `baseline+일간비상` : 주간 판정은 현행 그대로 + 구간 중 매일 **−16%(정상의 2배)** 감시,
    걸리면 그날 청산하고 **다음 주간 리밸런싱까지 현금**(사용자가 제안한 비대칭 그대로).
    참고로 2026-08-19 실측 DD(−9.95%)는 여기 못 미쳐 **그 주엔 발동하지 않는다** —
    지난주 결과에 맞춰 고른 값이 아님을 밝혀 둔다.
  *첫 채점(표본 1, 판정 아님):* 08-10→08-18 구간에서 `σ임계가드` **+7.01%**,
  나머지 전 후보 0.00%(현금), 벤치 +10.47%. σ 기준이었다면 08-10에 이미 재진입해
  반등을 일부 탔다는 뜻. **일간비상은 주간이 BULL일 때만 갈리므로 08-18~08-24부터** 갈린다.
  *사후 변경 방지:* 각 가드의 사전고정 파라미터를 매주 원장 `guard_specs`에 박는다.
  *회귀 검증:* 기존 4개 후보의 구간별·누적 수치 변화 0(테스트로 소수 12자리 등가성 고정).
  **▶ 2026-08-22 최초 실발동 관측(판정 아님, 표본 1).**
  8/14 종가 기준 domestic_dd **−2.60%** → BULL → 8/18(화, 광복절 대체휴일 회피 일회성 실행)
  전액 재매수. 예측대로 **지수 회복이 아니라 고점 망각**이었다: 20일 창의 최댓값이 112,999
  (7/23)로 밀려나 −2.60%가 됐을 뿐, **절대고점(6/25 145,532) 대비로는 여전히 −24.4%**.
  *3거래일 사후(8/21 종가) 채점 — 방향은 아직 이 가드에 유리:*
  현금 유지 0.00% vs 실제 재진입 −2.3%(약 −6.1만원)이나, 같은 기간 KODEX200 +1.32%라
  **재진입 판단(현금→주식) 자체는 틀리지 않았고 손실은 #OPEN-B(대리지표 괴리)에서 나왔다.**
  직전 4주 대피 기간의 기회비용(가드효과 누적 −4.09%p, 8/10→8/18 한 주만 −7.21%p)이
  이 −2.3%보다 훨씬 크다. → **3일로 판정 금지.** 섀도우 원장 8/24 갱신분부터
  `baseline` vs `baseline+콤보가드` 갈림을 계속 볼 것.
**[검증 인프라 / 보류(fail-safe라 우선순위 낮음)]**
- **#OPEN-V 검증 격차(스키마)** — KIS 응답 필드명 정확성(fix17/18류)이 **V1(mock 순환)로만**
  검증됨. 이 버그 클래스는 V1으로 원리적으로 못 잡는다(실사고 2026-07-09 hldg_qty). → **V5 골든
  응답 대조**(docs/에 실캡처 JSON 확보) 또는 **V6 실콘솔**로 승격 필요. 코드 버그가 아니라 검증
  인프라 과제. 다음에 KIS 실응답을 캡처할 기회(주간분석 S3 sync 등)에 docs/에 저장해두면 착수 가능.
- **#OPEN-1 매수 체결확인 없음** (korea.py) — 지정가 매수는 접수=완료로 간주. 미체결 시
  잔여현금 재투입 계산이 과소. 완전 해결은 매수도 체결폴링 필요(비용·복잡도↑) → 보류.
  *판단:* fail-safe(돈이 없어지진 않음, 다음 주 재조정으로 수렴)라 우선순위 중.
  **▶ 2026-08-23 fix33 — 관측은 붙었다. 집행부 보정은 여전히 미도입(항목 유지).**
  `rambdaB/execution_audit.py` 신설. 주문 전/후 잔고 차분으로 **실제 체결수량**을
  FULL/PARTIAL/NONE으로 판정해 `latest_signal.execution_audit`에 기록한다.
  이제 접수(ok)와 체결이 아카이브에서 분리되므로, 주간 성과가 나빴을 때
  "가드 판단이 틀렸나 / 체결이 나빴나"를 사후에 가를 수 있다(2026-08-22에 막혔던 지점).
  *새 TR_ID를 쓰지 않은 이유:* 주식일별주문체결조회는 저장소 내 사용처 0건 + 실캡처 0건이라
  CLAUDE.md 목킹 원칙 3번 상황 — 필드명을 추측하면 2026-07-09 `hldg_qty` 사고의 재판이다.
  검증된 잔고조회(TTTC8434R)의 `pdno`/`hldg_qty`만으로 설계했다.
  *남은 격차:* ① 매입평균가 필드명은 **여전히 미검증**(best-effort, 없으면 null).
  ② 관측일 뿐 재투입 예산 계산은 보정하지 않는다 — 그건 매매 로직 변경이라 별건.
  → 다음 실전 실행(08-24) 아카이브의 `execution_audit.balance_output1_keys`로
  진짜 필드명을 확인하면 **#OPEN-V(스키마 V5)의 첫 실캡처 근거**가 생긴다.
- **#OPEN-D 일일 실행을 붙이면 주간 분석 파이프라인이 오염된다** (신규 2026-08-23, 미착수) —
  사용자가 검토 중인 "일 1회 비상 체크" 구조의 통합 리스크. `scripts/analyze_returns.py:48`은
  `latest_signal/*.json`을 **전부 '정기 실행 기록'으로** 읽는다(필터는 `force_test_mode`와
  총자산 존재 여부뿐). 일일 산출물을 같은 prefix에 쓰면 **매일이 하나의 수익률 구간**이 되어
  주간 상대수익률·섀도우 원장·`prev_equity` 추적이 통째로 무너진다.
  *조치(착수 전 필수):* 일일 실행은 반드시 별도 prefix(`daily_check/`)로 격리.
  *연결:* L6ⓔ(소비자 계약) 미해결 렌즈가 정확히 이 클래스다.
- **#NOTE force_run 재실행 주간수익률 오염** — 같은 날 force_run 2회 시 prev_equity가 1회차가 쓴
  오늘값을 읽어 weekly_return≈0%. 테스트/수동 전용이고 실전은 주1회라 무영향 → fix 안 함, 인지만.

### 처리 완료 이력 (근본원인 → 조치)
| # | 발견(근본원인) | 조치 | 버전 | 검증강도 |
|---|---|---|---|---|
| fix14 | KIS 잔고조회 `rt_cd` 미검증 → 조용한 0원 | rt_cd 검증+3회 재시도, fallback 총자산 | 20260705 | V2 |
| fix17/18 | 보유수량/매수가능액 필드 오독 | `hldg_qty`/`nrcvb_buy_amt` 교정 + 테스트 | — | V1 (스키마 V5/V6 필요) |
| fix19 | VIX가 국내 단독 폭락에 장님 | KODEX200 20일 DD 가드(OR 결합) | 20260716.1 | V1 |
| fix22 | 야후 stale/부족 데이터로 가드 오판, 테스트가 실아카이브 오염, 실행버그 무방비 | data_guard 신선도검증 + s3_keys 격리 + CI 배포게이트 | 20260803.1 | V2 |
| fix23 | 주문/가격 함수 중복(수동 동기화 잠복버그), fetch_total_equity rt_cd 미검증 | kis_common 단일소스 + 하드닝 + identity 테스트 | 20260803.2 | V3 |
| fix24 | 배치 부분 실패 → 반쪽 유니버스로 실매수 | 커버리지 가드(70%) → fail-safe 중단 | 20260803.3 | V2 |
| fix25 | stale VIX로 BULL 오판(미국발 폭락 fail-open) | fetch_vix validate_prices 검증 → stale→UNKNOWN | 20260803.4 | V2 |
| fix26 | 모멘텀 current 종목별 stale → 옛날가 top10 편입 | current validate_prices 검증 → 미달 종목 제외 | 20260803.5 | **V4** (경계 스윕) |
| fix27 | 재투입 수학 무검증(💰💰💰 V0) — 버그는 없었음 | reinvest 돈 불변식 테스트(과투입 금지·종목당 상한) | 20260803.6 (테스트만) | **V4** (속성 스윕) |
| fix28 | 메인 사이징 무검증(💰💰💰 V0) — 버그는 없었음 | run_korea_rebalancing e2e(균등배분·과지출 금지·순위 이탈·히스테리시스) | 20260804.1 (테스트만) | **V4** (e2e 불변식) |
| fix29 | 오케스트레이터 안전장치 무검증(💰💰💰 V0) — 버그는 없었음 | lambda_handler e2e(중복가드·CASH_RESERVE·BEAR 분기) | 20260804.2 (테스트만) | **V4** (e2e 안전장치) |
| fix30 | **[L6]** 조용한 스킵(STALE/S3_ERROR/NO_TARGETS)로 매매 미실행을 사용자가 모름 | 세 스킵에 텔레그램 경고(관측성), MARKET_CLOSED는 침묵 | 20260804.3 | **V4** (e2e 스윕) |
| fix31 | **[L6ⓔ]** 섀도우 원장이 ①현금대피 중 진짜 0%를 결측 처리(`e1!=e0` 금액비교) ②결측을 `+0.00%`로 텔레그램 보고(초기값 1.0 노출) — 관측 지표가 조용히 거짓 | 판정을 금액→**스냅샷 날짜**(`t0!=t1`)로 교정, `account_intervals` 추가, 채점 0개면 `account_pct=None`+"데이터 없음" 표기 | 20260810.1 | **V2** (변이-구별: 옛 로직 주입 시 3건 검출) |
| fix32 | **[L6ⓓ]** 순위 신호 품질(#OPEN-S)·가드 대리지표 상관(#OPEN-B)이 **한 번도 측정되지 않아** 열화해도 알 수 없음 | `signal_quality.py` 주간 관측(IC·분위·스프레드·상관) → 루트 누적원장 + 텔레그램. **판정은 26주 사전고정 기준**으로만(노이즈 추종 차단) | 20260810.2 | **V2** (변이-구별: 과최적화 방벽 제거 시 3건 검출) |
| fix33 | **[L6ⓓ]** 접수(ok)와 체결이 구분 안 돼 손실 귀속 불가(#OPEN-1) + 재진입 대안 가드를 채점할 장치 없음(#OPEN-R) | `execution_audit.py`(잔고 차분으로 FULL/PARTIAL/NONE, fail-safe·FORCE_TEST_MODE 스킵) + `guards.py`(σ임계·일간비상 후보, 임계 역산·원장 박제). **매매 로직 무변경** | 20260823.1 | **V3** (기존 후보 등가성 소수 12자리 고정 + 실패경로 전수) |

### 검증된 fail-safe 체인 (감사 시 재확인용)
- A 실패/중단(500) → `quant_signals.json` 미갱신 → B가 `updated_at` 나이 > `SIGNAL_MAX_AGE_SECS`
  감지 → `STALE_SIGNAL_ABORT` → **기존 포지션 유지**. (A쪽 결함을 "중단"으로 처리해도 안전한 근거)
- VIX 수집 실패 → carry-over → 그것도 없으면 UNKNOWN → B `VIX_UNKNOWN_SKIP` + 텔레그램 경고.
- force_bull 시그널을 실전모드 B가 받으면 → `FORCE_BULL_IN_LIVE_MODE_ABORT` 주문 없이 중단.
- 매도 미체결(예수금 0) → 매수 전체 스킵(없는 돈으로 매수 방지).

---

## ③ 주간 신호 검증 루프 (순위 예측력 — 매주 기록, 누적으로만 판정)

> **목적:** "우리가 매기는 순위가 실제로 다음 주 수익을 예측하는가"를 매주 측정해
> **신호 열화를 조기에 감지**한다. 감사(②)가 *코드 결함*을 잡는다면, 이 루프는 *신호 품질*을 본다.
>
> ⚠️ **이 루프의 출력으로 매주 파라미터를 고치지 않는다.** IC는 주간 표준편차 0.398로
> **노이즈가 신호보다 크다**(2026Q2 +0.273 → Q3 −0.415로 국면마다 뒤집힘). 한 주 빗나갔다고
> 코드를 고치면 노이즈를 쫓게 되고, 그것이 [[signal-tuning-freeze]]가 기록한 **3연속 검증 탈락**의
> 원인이었다. 그래서 **기록은 매주, 판정은 누적, 수정은 섀도우 통과 후**로 분리한다.

### STEP A — 매주 기록 (예측을 먼저 남긴다)
1. 이번 주 `universe/*.json` 스냅샷에서 **전체 유니버스 순위**(momentum 내림차순)를 확보한다.
   → 이것이 **이번 주의 예측**이다. 반드시 결과를 보기 **전에** 기록한다(사후 합리화 차단).
2. 직전 주 스냅샷 순위 ↔ **그 주에 실현된 수익**을 대조해 다음을 계산한다.
   - **IC**(순위상관: momentum 순위 vs 실현수익 순위) — 양수면 예측 성공
   - **분위별 평균수익**(Q1~Q5) — 단조 감소해야 정상
   - **top10 − 유니버스 스프레드(%p)** — 실제로 얻은 초과수익
3. 위 3개와 **그 주 market_status(BULL/BEAR)**를 원장에 append 한다.
   > BEAR 주는 실제로 매수하지 않으므로 **"예측은 틀렸지만 손실은 안 났다"**가 정상이다.
   > IC와 실현손익을 혼동하지 말 것(2026-08-03 주가 정확히 그 사례).

### STEP B — 판정은 누적으로만 (기준은 사전 고정)
- **최소 표본 26주** 이전에는 어떤 판정도 하지 않는다. 경향 언급만 가능.
- 아래 **사전 정의된 기준**이 발동할 때만 "신호 열화"로 본다. *기준을 사후에 바꾸지 않는다.*
  - `IC 26주 이동평균 < 0` 이 **4주 연속**, **또는**
  - `top10−유니버스 스프레드 26주 누적 < 0`
- 기준 미발동이면 **아무것도 하지 않는다.** "IC가 낮아 보인다"는 수정 사유가 아니다.

### STEP C — 발동했을 때만, 그리고 섀도우를 먼저
1. 원인을 **국면**(급반전기엔 모멘텀이 원래 무너짐)과 **구조적 열화**(유니버스 성격 변화 등)로
   구분한다. 국면 탓이면 대기한다 — 모멘텀의 알려진 특성이지 결함이 아니다.
2. 구조적 열화로 판단되면 **프로덕션이 아니라 `strategies/`에 후보를 추가**해 섀도우에서 채점한다.
   (리스크조정 아이디어는 이미 `voltilt`로 채점 중 — 새로 만들기 전에 기존 후보부터 확인)
3. **섀도우 OOS에서 여러 달 우월**해야 승격을 논한다. 백테스트 숫자로 채택 금지.
4. 승격 시 ②원장에 fix 항목으로 기록하고 SPECIFICATION(핵심 전략 로직)도 갱신한다.

### 현재 상태 — **구현 완료(fix32, 2026-08-10)**
- `backtest/signal_quality.py` — `SignalQualityAnalyzer`(IC·분위·스프레드) /
  `ProxyCorrelationMonitor`(top10↔KODEX200 상관·베타) / `SignalQualityLedger`(누적·판정·경보).
- 원장 **`signal_quality_ledger.json`(저장소 루트)** — `data/`는 .gitignore라 누적형 원장은
  루트에 둬야 CI에서 표본이 쌓인다. 매주 GitHub Actions가 append 후 커밋.
- 주간 텔레그램 성적표에 "신호 품질" 섹션 자동 추가(판정 보류 중엔 진행도만 표시).
- 발동 기준은 코드 상수로 고정(`MIN_SAMPLE_WEEKS=26`, `IC_NEGATIVE_STREAK=4`,
  `PROXY_CORR_FLOOR=0.80`)이고 원장의 `criteria`에 기록된다 — **사후 변경 시 테스트가 감지**한다.
- 누적 현황: **1/26주** (2026-08-10 기준). 판정은 26주 이후.

---
*이 문서는 감사할 때마다 ②를 갱신한다. ①(방법)과 ③(루프 절차)은 원칙이 바뀔 때만 고친다.*

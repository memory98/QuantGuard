# AUDIT.md — QuantGuard 비판적 재감사 런북

> **언제 이 문서를 쓰나:** 사용자가 "비판적으로 봐줘 / 문제점 찾아줘 / 분석 평가 / 안정성 점검"
> 등을 요청할 때. **매번 처음 보는 사람처럼, 문제를 찾는 것 위주로** 감사한다.
> 목표는 새 기능이 아니라 **실돈을 지키는 안정성 완성**이다.
>
> 이 문서는 두 부분이다 — ① **감사 방법(고정 절차)**, ② **발견 이력 원장(매번 갱신)**.
> 원장이 있어서 같은 곳을 두 번 훑지 않고, 아직 안 본 곳·미해결 항목으로 바로 간다.

---

## ① 감사 방법 (매번 이 순서)

### 원칙 (마인드셋)
1. **처음 보는 눈.** 기존 주석의 "이건 fix13에서 고쳤음"을 믿지 말고 코드가 실제로 그렇게
   동작하는지 실행경로를 직접 읽어 확인한다.
2. **돈-리스크 우선.** 신호 정확도보다 **실행부 버그가 실돈을 더 크게 날린다.** 감사 순서는
   아래 컴포넌트 리스크 맵의 💰 높은 순.
3. **fail-safe 검증 필수.** 결함을 고치기 전에 "이 경로가 실패하면 시스템이 안전한 쪽으로
   끝나는가"를 먼저 확인한다. (예: A가 중단→시그널 미갱신→B가 `STALE_SIGNAL_ABORT`로
   포지션 유지 = 안전). 고치는 방향도 **fail-safe**(의심스러우면 매매 스킵)로 잡는다.
4. **신호/전략 파라미터는 동결.** 모멘텀 기간·임계값·재진입·콤보는 백테스트 3연속 검증
   탈락 이력이 있어 **손대지 않는다.** 감사에서 고치는 건 *신뢰성*(가드·검증·중복제거)만.
   ([[signal-tuning-freeze]] 참조)
5. **한 증상만 고치고 끝내지 않는다.** 오매칭 고치면 미매칭 회귀 확인, 그 반대도.
   실제 운영 데이터로 수정 전/후 비교 전엔 완료 처리 금지. (CLAUDE.md "수정 완료 판정 기준")

### 절차 (STEP)
- **STEP 0.** 이 문서 ②원장을 읽어 **이미 감사한 곳 / 미해결·보류 항목**을 파악한다.
  미해결 항목이 있으면 거기부터, 없으면 아래 맵에서 **가장 오래 안 본 💰 컴포넌트**를 고른다.
- **STEP 1.** 그 컴포넌트의 **실행경로를 실제로 정독**한다(grep로 함수 위치 → Read).
  아래 "버그 클래스 체크리스트"를 대조하며 읽는다.
- **STEP 2.** 의심 결함마다 **구체적 실패 시나리오**(입력→잘못된 출력)를 적는다. 시나리오를
  못 만들면 결함이 아니다(추측 금지).
- **STEP 3.** 결함의 **fail-safe 여부**를 확인한다. 이미 안전하게 끝나면 우선순위를 낮춘다.
- **STEP 4.** 고칠 것을 **하나** 골라(가장 큰 돈-리스크 × 낮은 변경위험) 수정 + **회귀 테스트**
  작성. 테스트는 **실제 사고 시나리오를 재현**해야 한다(통과만 하는 테스트 금지).
  외부 API mock 필드는 코드에서 베끼지 말고 독립 근거로(CLAUDE.md "외부 API 목킹 원칙").
- **STEP 5.** 전체 테스트 통과 + `py_compile` → `/log/` JSON → 커밋/푸시(CI 게이트) →
  Notion HISTORY 새 항목 → (안전장치/로직 변경 시) SPECIFICATION 갱신.
- **STEP 6.** **이 문서 ②원장을 갱신** — 발견 항목 추가, 감사 커버리지 날짜 갱신,
  보류 항목 정리. (이걸 빼먹으면 다음 감사가 같은 곳을 또 훑는다.)

### 버그 클래스 체크리스트 (읽으며 대조)
- [ ] **조용한 부분 실패** — 루프에서 `except: continue` 후 결과 개수/커버리지 검증 없이 진행?
      (fix24 사고: 배치 절반 실패해도 반쪽으로 매수)
- [ ] **신선도(stale) 미검증** — 데이터가 *있지만 낡은* 경우를 그냥 쓰나? (empty/예외만 잡고
      오래된 마지막 값 사용) → 잘못된 BULL/BEAR·모멘텀. (fix22가 DD가드만 커버, 모멘텀·VIX 미해결)
- [ ] **응답코드 미검증** — KIS `rt_cd`를 확인 안 하고 `output`만 읽나? 유량초과 응답을
      정상처럼 반환? (fix14/23 하드닝 대상)
- [ ] **체결 가정** — 주문 접수(rt_cd=0)를 체결로 간주하나? 미체결 시 후속 계산이 틀어지나?
      (매도는 체결폴링 있음, **매수는 접수=완료로 간주 — 미해결 #OPEN-1**)
- [ ] **fail-open** — 데이터/조회 실패 시 위험한 쪽(BULL 개방·전량 매수)으로 기본값? (fix15 VIX)
- [ ] **나눗셈/인덱스 edge** — 0 나눗셈, 빈 시리즈 `.iloc[-1]`, base==0, 데이터 1행.
- [ ] **타임존/기준일** — `datetime.today()`(UTC Lambda) vs KST, `last_friday` 요일 계산,
      `<= last_friday` 필터 일관성.
- [ ] **중복/동기화** — 같은 로직이 두 곳에 복붙돼 수동 동기화 중? (fix23 단일소스화)
- [ ] **테스트 격리** — 테스트 실행이 실전 아카이브/시그널을 덮나? (fix22 s3_keys)
- [ ] **회귀 방향** — 정밀도 고치며 재현율 깨지 않았나(그 반대도)? 실운영 데이터로 비교했나?

---

## ② 발견 이력 원장 (매번 갱신) — 최종 갱신 2026-08-03

### 컴포넌트 리스크 맵 & 감사 커버리지
| 컴포넌트 | 돈-리스크 | 최근 감사 | 상태 |
|---|---|---|---|
| `rambdaB/korea.py` (매도/매수 집행) | 💰💰💰 | 2026-08-03 | 감사됨(fix14/17/18/23). **매수 체결확인 미해결 #OPEN-1** |
| `rambdaB/lambda_function.py` (오케스트레이션) | 💰💰💰 | 2026-08-03 | 감사됨(fix23 fetch_total_equity 하드닝) |
| `rambdaB/kis_common.py` (공통 주문/가격) | 💰💰💰 | 2026-08-03 | 감사됨(fix23 단일소스) |
| `rambdaA/signal_generator.py` (모멘텀·가드) | 💰💰 | 2026-08-03 | 감사됨(fix24 커버리지). **모멘텀·VIX stale 미해결 #OPEN-2** |
| `rambdaA/yf.py` (야후 수집) | 💰💰 | — | **미감사** — 단일 데이터 소스, 파싱/range 하드코딩 리스크 |
| `rambdaA/fdr.py` (네이버 ETF 목록) | 💰💰 | 부분 | 부분(fix20 정렬). 전면 미감사 |
| `rambdaA/data_guard.py` / `s3_keys.py` | 💰 | 2026-08-03 | 감사됨(fix22) |
| `dashboard/`, `paper_trader/`, `backtest/`, `scripts/` | 🚫 종이/분석 | — | 실돈 아님 — 후순위 |

> **다음 감사 1순위:** #OPEN-2(모멘텀·VIX stale) → 그다음 `rambdaA/yf.py` 정독(미감사 💰💰).

### 미해결 / 보류 항목 (다음 감사에서 우선 검토)
- **#OPEN-1 매수 체결확인 없음** (korea.py) — 지정가 매수는 접수=완료로 간주. 미체결 시
  잔여현금 재투입 계산이 과소. 완전 해결은 매수도 체결폴링 필요(비용·복잡도↑) → 보류.
  *판단:* fail-safe(돈이 없어지진 않음, 다음 주 재조정으로 수렴)라 우선순위 중.
- **#OPEN-2 모멘텀 `current`·VIX stale 미검증** — fix22 신선도 가드가 DD가드에만 걸림.
  데이터가 있으나 낡은 경우 잘못된 랭킹/가드 가능. VIX는 야후가 대개 에러로 실패해 fix15
  UNKNOWN 경로가 잡고, 모멘텀 동시 stale은 fix24 커버리지가 일부만 커버. → **다음 1순위.**
  *주의:* 신호 파라미터가 아니라 신뢰성 가드라 동결 정책과 무관하게 손대도 됨.

### 처리 완료 이력 (근본원인 → 조치)
| # | 발견(근본원인) | 조치 | 버전 |
|---|---|---|---|
| fix14 | KIS 잔고조회 `rt_cd` 미검증 → 조용한 0원 | rt_cd 검증+3회 재시도, fallback 총자산 | 20260705 |
| fix17/18 | 보유수량/매수가능액 필드 오독 | `hldg_qty`/`nrcvb_buy_amt` 교정 + 테스트 | — |
| fix19 | VIX가 국내 단독 폭락에 장님 | KODEX200 20일 DD 가드(OR 결합) | 20260716.1 |
| fix22 | 야후 stale/부족 데이터로 가드 오판, 테스트가 실아카이브 오염, 실행버그 무방비 | data_guard 신선도검증 + s3_keys 격리 + CI 배포게이트 | 20260803.1 |
| fix23 | 주문/가격 함수 중복(수동 동기화 잠복버그), fetch_total_equity rt_cd 미검증 | kis_common 단일소스 + 하드닝 + identity 테스트 | 20260803.2 |
| fix24 | 배치 부분 실패 → 반쪽 유니버스로 실매수 | 커버리지 가드(70%) → fail-safe 중단 | 20260803.3 |

### 검증된 fail-safe 체인 (감사 시 재확인용)
- A 실패/중단(500) → `quant_signals.json` 미갱신 → B가 `updated_at` 나이 > `SIGNAL_MAX_AGE_SECS`
  감지 → `STALE_SIGNAL_ABORT` → **기존 포지션 유지**. (A쪽 결함을 "중단"으로 처리해도 안전한 근거)
- VIX 수집 실패 → carry-over → 그것도 없으면 UNKNOWN → B `VIX_UNKNOWN_SKIP` + 텔레그램 경고.
- force_bull 시그널을 실전모드 B가 받으면 → `FORCE_BULL_IN_LIVE_MODE_ABORT` 주문 없이 중단.
- 매도 미체결(예수금 0) → 매수 전체 스킵(없는 돈으로 매수 방지).

---
*이 문서는 감사할 때마다 ②를 갱신한다. ①(방법)은 원칙이 바뀔 때만 고친다.*
